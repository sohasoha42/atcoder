{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug C++ (LLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "cwd": "${fileDirname}",
            "args": [],
            "preLaunchTask": "build-cpp"
        }
    ]
}